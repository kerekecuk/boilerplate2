Порядок установки:

1. npm install typescript --save
2. npm install react react-dom --save
3. npm install webpack webpack-cli --save
4. npm install babel-core babel-loader babel-preset-env babel-preset-react --save
5. npm install html-webpack-plugin --save